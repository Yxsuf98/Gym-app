create database ml;
use database ml;

create table form(
    id int primary key;
    name varchar(30);
    membership_status varchar(1);
    age int;
    date_issued Varchar(20);
    date_expired varchar(20);
    typ_of_membership varchar(20);

);


insert into form values();
insert into form values();


alter table form();
alter table form(;)





select * 
from form
where EXISTS;
